# 协作规范（定稿 2026-07-11）

1. 所有代码与知识库数据统一进本私有仓库管理，完整提交历史就是作品原创性与可复现性的证据。
2. main 分支保护，所有改动走分支 + PR 合入，不直接往 main 推。
3. 每个 PR 对应一个 Issue。大任务的 Issue 先写三行：要解决什么、这期不做什么、怎么算完成；小任务一句话。设计讨论写在 Issue 评论里。
4. PR 写清：改了什么、为什么这么改；改动范围和 Issue 一致，不夹带无关改动，不堆大包。
5. 合并前请一位同学过目，只看三件事：是 Issue 说的事吗？有没有夹带？看懂了吗？
6. 方案和接口约定开工即定稿；要变更就新写一条说明"改了什么、为什么、影响哪里"，不覆盖旧文档。
7. API key、appid 这类密钥只放本地 .env，任何时候不进仓库。

写代码、打 git 命令都可以让 AI（Cursor、Codex、Claude Code 等）代劳，把本规范贴给它就行——但 PR 挂的是我们自己的名字：改了什么、为什么改，自己要能讲清。首次配置见 docs/onboarding.md。
