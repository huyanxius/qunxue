# 七维知识库

本目录保存社会学七维框架的可读知识源：五个内部维度，以及 D6 学派传统、D7 学科史。Markdown 是后续评审、diff 和程序解析的主格式；`source/` 中的 DOCX 仅作为冻结溯源快照，提交后不再修改。内容修订应直接改 Markdown，不应回头覆盖 Word 快照。

## 五个维度

| 维度 | 编号规则 | 条目数 | 内容范围 |
| --- | --- | ---: | --- |
| 本体论 | `C` + 三位以上数字，如 `C001` | 1067 | 社会学研究对象、知识概念及其历史演变 |
| 认识论 | `E` + 三位数字，如 `E001` | 124 | 社会学问题的前提、认识立场与知识成立条件 |
| 价值论 | `V` + 三位数字，如 `V001` | 304 | 社会学的研究目的、价值取向和公共使命 |
| 方法论 | 连续唯一的 `M001`–`M690` | 690 | 定性、定量、比较、计算等研究方法及其演变 |
| 实践论 | 连续唯一的 `P001`–`P370` | 370 | 研究设计、资料获取、分析、写作与质量控制等操作流程 |
| **合计** |  | **2555** |  |

注意：实践论源稿实际使用 `P001`–`P141` 和 `R142`–`R370` 两组前缀；方法论源稿存在重复编号。转换器按正文顺序直接把方法论统一为 `M001`–`M690`，并把实践论统一为 `P001`–`P370`。不生成旧编号映射，冻结 DOCX 不受影响。

## D6 与 D7

| 维度 | 编号规则 | 条目数 | 拆分方式 |
| --- | --- | ---: | --- |
| D6 学派传统 | 人物条目 `P001`–`P218` | 218 | 53 个学派块加 10 个缺少学派层级的地区块，另存目录说明 |
| D7 学科史 | 编年条目 `H001`–`H091` | 91 | 按 91 个编年条目拆分，另存说明与分组 |
| **D6/D7 合计** |  | **309** |  |

七维条目总计为 **2864**。D6 的 `P` 编号与实践论的 `P` 编号属于不同维度目录，编号按维度作用域解释。

D6 人物标题按正文顺序统一编号时，正文中出现的对应人物编号引用也会同步直接替换；不生成旧编号映射文件。

## 目录结构

```text
knowledge/
├── README.md
├── convert.py
├── 转换报告.md
├── source/
├── 本体论/
├── 认识论/
├── 价值论/
├── 方法论/
├── 实践论/
├── 学派传统/
└── 学科史/
```

每个维度按 Word 中的标题层级拆分：`#` 为文档标题、`##` 为大类、`###` 为子类、`####` 为条目、`#####` 为时段。价值论部分大类下直接出现条目，脚本会为该大类生成文件，不会因缺少子类而丢数据。为保留大类导言，含子类的大类也会单独生成一个大类文件。

## 重新转换

要求 Python 3.10+ 和 Pandoc。生成本快照时使用 Pandoc 3.6.1；转换参数固定为 `-f docx -t markdown-smart --wrap=none`。

从仓库根目录运行：

```bash
python3 knowledge/convert.py <源目录> knowledge
```

源目录支持本批材料的原始文件名，也支持 `<维度>全量.docx` 命名。脚本会：

1. 将五份内部维度及 D6、D7 DOCX 转成 Markdown；
2. 五维按大类/子类拆分，D6 按学派拆分，D7 按编年条目拆分；
3. 逐字节复制源 DOCX 到 `source/`；
4. 核对条目总数、重复编号和前缀；
5. 重新生成 `转换报告.md`。

如果 `pandoc` 不在 `PATH` 中，可设置 `PANDOC` 环境变量为可执行文件的绝对路径。报告不含运行时间；同一 Pandoc 版本、同一源文件和同一脚本重复执行时，输出应逐字节一致。

条目数不符、重复编号或前缀异常会让脚本在写完 Markdown 和报告后返回非零退出码。方法论和实践论的编号统一在核对前自动执行，因此正常输出应通过唯一性与前缀检查。

## 源材料说明

五维正文来自以下文件：

- `社会学知识概念历史脉络结构——本体论工程.docx`
- `社会学问题前提历史脉络结构——认识论工程.docx`
- `社会学研究目的历史脉络结构——价值论工程.docx`
- `社会学研究方法历史脉络结构——方法论工程.docx`
- `社会学研究操作历史脉络结构——实践论工程.docx`

`D6_学派传统.docx` 与 `D7_学科史.docx` 同时保留冻结快照并生成 Markdown，但不计入内部五维 2555 条。当前材料中没有名为《纲要7》的独立文件，因此 `D7_学科史.docx` 仍按正文标题“学科史”命名。

本批五份内部维度稿与 Issue 描述的“观点—文献依据 / 文献 / 支持范围 / A-B-C 证据等级”结构不一致：实际正文没有这些独立字段。脚本会在转换报告中如实记录为零，避免误报为转换器漏行。认识论文件数与时段结构保留原样；方法论重复编号和实践论前缀混用在 Markdown 层直接统一，并在报告中记录处理数量。
